package io.github.rreeggkk.nuclearsciences.client.gui.util;

import net.minecraft.client.renderer.texture.TextureManager;
import net.minecraftforge.fluids.FluidStack;

public class GuiUtil {
	public static void drawFluid(FluidStack fluid, TextureManager manager) {
		//ResourceLocation loc = fluid.getFluid().getStill(fluid);
		//ITextureObject text = manager.getTexture(loc);
	}
}
